# dummy-repo-rust

This is a dummy repository for Rust, mirroring the structure of the Python version provided in the image.

## Setup

1. Install Rust: https://www.rust-lang.org/tools/install
2. Run with: `cargo run`
